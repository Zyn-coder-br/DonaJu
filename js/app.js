const state={view:'dashboard',products:[{name:'Brownie tradicional',cost:5.5,price:10,stock:50},{name:'Brownie recheado',cost:6.5,price:12,stock:30}],customers:[{name:'Maria da Silva',phone:'(00) 00000-0000'}],orders:[{id:'PED-001',customer:'Maria da Silva',date:'2026-09-22',status:'Agendado',total:160,items:15}]};
const $=s=>document.querySelector(s);const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v);const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function setView(view){state.view=view;document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.view===view));$('#page-title').textContent={dashboard:'Dashboard',orders:'Pedidos',products:'Produtos',customers:'Clientes',deliveries:'Entregas',reports:'Relatórios'}[view];render()}
function render(){const views={dashboard:dashboard,orders:orders,products:products,customers:customers,deliveries:deliveries,reports:reports};$('#app-view').innerHTML=views[state.view]();bindDynamic()}
function dashboard(){const revenue=state.orders.reduce((a,o)=>a+o.total,0);const units=state.orders.reduce((a,o)=>a+o.items,0);return `<div class="grid grid-4"><div class="card"><div class="metric-label">Faturamento registrado</div><div class="metric-value">${money(revenue)}</div><div class="metric-foot">Base demonstrativa</div></div><div class="card"><div class="metric-label">Pedidos</div><div class="metric-value">${state.orders.length}</div><div class="metric-foot">Encomendas cadastradas</div></div><div class="card"><div class="metric-label">Unidades vendidas</div><div class="metric-value">${units}</div><div class="metric-foot">Brownies registrados</div></div><div class="card"><div class="metric-label">Produtos no estoque</div><div class="metric-value">${state.products.reduce((a,p)=>a+p.stock,0)}</div><div class="metric-foot">Saldo demonstrativo</div></div></div><div class="grid grid-2"><div class="card"><div class="section-head"><h2>Vendas por semana</h2><span class="pill">Exemplo</span></div><div class="chart">${[35,58,42,78,65,90,72].map((h,i)=>`<div class="bar" style="height:${h}%"><span>S${i+1}</span></div>`).join('')}</div><p class="muted" style="font-size:12px">O gráfico será conectado aos dados reais após a integração com o Supabase.</p></div><div class="card"><div class="section-head"><h2>Próximas entregas</h2><span class="pill yellow">Agenda</span></div>${state.orders.slice(0,4).map(o=>`<div style="display:flex;justify-content:space-between;gap:12px;padding:13px 0;border-bottom:1px solid var(--line)"><div><strong>${esc(o.customer)}</strong><div class="muted" style="font-size:12px">${o.date} • ${o.items} unidades</div></div><span class="pill">${esc(o.status)}</span></div>`).join('')||'<div class="empty">Nenhuma entrega cadastrada.</div>'}</div></div><div class="card"><div class="section-head"><h2>Últimos pedidos</h2><button class="secondary-btn" data-action="orders">Ver todos</button></div>${orderTable()}</div>`}
function orderTable(){return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Pedido</th><th>Cliente</th><th>Entrega</th><th>Unidades</th><th>Total</th><th>Status</th></tr></thead><tbody>${state.orders.map(o=>`<tr><td>${o.id}</td><td>${esc(o.customer)}</td><td>${o.date}</td><td>${o.items}</td><td>${money(o.total)}</td><td><span class="pill">${esc(o.status)}</span></td></tr>`).join('')}</tbody></table></div>`}
function orders(){return `<div class="section-head"><h2>Encomendas</h2><button class="primary-btn" data-action="new-order">+ Novo pedido</button></div><div class="card">${orderTable()}</div>`}
function products(){return `<div class="section-head"><h2>Produtos prontos</h2><button class="primary-btn" data-action="new-product">+ Novo produto</button></div><div class="card"><div class="table-wrap"><table class="data-table"><thead><tr><th>Produto</th><th>Custo</th><th>Venda</th><th>Margem</th><th>Estoque</th></tr></thead><tbody>${state.products.map(p=>`<tr><td>${esc(p.name)}</td><td>${money(p.cost)}</td><td>${money(p.price)}</td><td>${money(p.price-p.cost)} <span class="muted">(${((p.price-p.cost)/p.price*100).toFixed(1)}%)</span></td><td><span class="pill ${p.stock<10?'yellow':'green'}">${p.stock}</span></td></tr>`).join('')}</tbody></table></div></div>`}
function customers(){return `<div class="section-head"><h2>Clientes</h2><button class="primary-btn" data-action="new-customer">+ Novo cliente</button></div><div class="card"><div class="table-wrap"><table class="data-table"><thead><tr><th>Nome</th><th>Telefone</th></tr></thead><tbody>${state.customers.map(c=>`<tr><td>${esc(c.name)}</td><td>${esc(c.phone||'—')}</td></tr>`).join('')}</tbody></table></div></div>`}
function deliveries(){return `<div class="section-head"><h2>Agenda de entregas</h2><button class="primary-btn" data-action="new-order">+ Agendar entrega</button></div><div class="card">${state.orders.map(o=>`<div style="display:flex;justify-content:space-between;gap:12px;padding:15px 0;border-bottom:1px solid var(--line)"><div><strong>${esc(o.customer)}</strong><div class="muted" style="font-size:12px">${o.date} • ${o.items} brownies</div><div class="muted" style="font-size:12px">Validade calculada após confirmação da entrega</div></div><span class="pill">${esc(o.status)}</span></div>`).join('')}</div>`}
function reports(){return `<div class="grid grid-2"><div class="card"><div class="section-head"><h2>Receita x custo</h2><span class="pill">Demonstração</span></div><div class="chart">${[45,60,52,70,66,82].map((h,i)=>`<div class="bar" style="height:${h}%"><span>${i+1}</span></div>`).join('')}</div><p class="muted" style="font-size:12px">A versão conectada exibirá comparativos mensais, margens e quantidade vendida.</p></div><div class="card"><div class="section-head"><h2>Indicadores</h2></div><p><strong>Receita:</strong> ${money(state.orders.reduce((a,o)=>a+o.total,0))}</p><p><strong>Pedidos:</strong> ${state.orders.length}</p><p><strong>Produtos:</strong> ${state.products.length}</p><p><strong>Clientes:</strong> ${state.customers.length}</p><p class="muted" style="font-size:12px">Os indicadores financeiros serão ampliados após a definição de pagamentos e despesas.</p></div></div>`}
function openModal(type){const configs={product:{title:'Novo produto',fields:[['name','Nome do produto','text'],['cost','Custo unitário','number'],['price','Preço de venda','number'],['stock','Estoque inicial','number']]},customer:{title:'Novo cliente',fields:[['name','Nome','text'],['phone','Telefone','text']]},order:{title:'Novo pedido',fields:[['customer','Cliente','text'],['date','Data de entrega','date'],['items','Quantidade de brownies','number'],['total','Valor total da venda','number']]}};const cfg=configs[type];$('#modal-root').innerHTML=`<div class="modal-backdrop"><div class="modal"><div class="modal-header"><h2>${cfg.title}</h2><button class="icon-btn" data-action="close">×</button></div><form id="modal-form"><div class="form-grid" style="margin-top:20px">${cfg.fields.map(([key,label,inputType])=>`<div class="field"><label for="${key}">${label}</label><input id="${key}" name="${key}" type="${inputType}" required></div>`).join('')}</div><div class="modal-actions"><button type="button" class="secondary-btn" data-action="close">Cancelar</button><button class="primary-btn" type="submit">Salvar demonstração</button></div></form></div></div>`;$('#modal-form').addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));if(type==='product')state.products.push({name:data.name,cost:Number(data.cost),price:Number(data.price),stock:Number(data.stock)});if(type==='customer')state.customers.push({name:data.name,phone:data.phone});if(type==='order')state.orders.push({id:'PED-'+String(state.orders.length+1).padStart(3,'0'),customer:data.customer,date:data.date,items:Number(data.items),total:Number(data.total),status:'Agendado'});$('#modal-root').innerHTML='';render()})}
function bindDynamic(){document.querySelectorAll('[data-action]').forEach(el=>el.addEventListener('click',()=>{const a=el.dataset.action;if(a==='new-product')openModal('product');if(a==='new-customer')openModal('customer');if(a==='new-order')openModal('order');if(a==='close')$('#modal-root').innerHTML='';if(a==='orders')setView('orders')}))}
document.querySelectorAll('.nav-item').forEach(b=>b.addEventListener('click',()=>setView(b.dataset.view)));$('#quick-add').addEventListener('click',()=>openModal('order'));render();
